<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sergiorodriguez.venalau">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReservasActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".IniciarSesionActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".ImplementosActivity" />
        <activity android:name=".EventosActivity" />
        <activity android:name=".DetalleEventoActivity" />
        <activity android:name=".DeportesHomeActivity" />
        <activity android:name=".ConfirmacionActivity" />
        <activity android:name=".EstadoCuentaActivity"></activity>
    </application>

</manifest>